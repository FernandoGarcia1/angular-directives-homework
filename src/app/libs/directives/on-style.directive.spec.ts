import { OnStyleDirective } from './on-style.directive';

describe('OnStyleDirective', () => {
  it('should create an instance', () => {
    const directive = new OnStyleDirective();
    expect(directive).toBeTruthy();
  });
});
